/** * Created by lzq on 2017/8/24. * webpack base config */const path = require('path');const utils = require('./../utils');const config = require('../../config/index');const vueLoaderConfig = require('./../vue-loader.conf.js');function resolve (dir) {    return path.join(__dirname, '../../', dir);}module.exports = {    output:{        path:config.build.assetsRoot,  // build2 目录        filename: '[name].js',        publicPath: process.env.NODE_ENV === 'production'            ? config.build.assetsPublicPath            : config.dev.assetsPublicPath    },    resolve:{        extensions:['.js','.vue','.json'],        alias: {            'vue$': 'vue/dist/vue.esm.js',            '@': resolve('src')        }    },    module:{        rules:[            {                test: /\.vue$/,                loader: 'vue-loader',                include:[resolve('src'),resolve('example')],                options: vueLoaderConfig            },            {                test: /\.js$/,                loader: 'babel-loader',                include: [resolve('src'),resolve('example')]            },            // 不需要如此处理，因为最后在部署到生产环境还是要处理的            {                test: /\.(png|jpe?g|gif|svg)(\?.*)?$/,                loader: 'url-loader',                //include:[resolve('src')],                options: {                    limit: 81920,                    name: utils.assetsPath('static/[name].[ext]')                }            },            {                test: /\.(woff2?|eot|ttf|otf)(\?.*)?$/,                loader: 'url-loader',                options: {                    limit: 8192,                    name: utils.assetsPath('fonts/[name].[hash:7].[ext]')                }            }        ]    }};