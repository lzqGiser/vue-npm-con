/** * Created by lzq on 2017/9/5. * 用来生成example内容 */const webpackPubConfig = require('./webpack.pub.conf');const config = require('../../config');const merge = require('webpack-merge');const HtmlWebpackPlugin = require('html-webpack-plugin');const webpackExpConfig = merge(webpackPubConfig,{    entry:{        index:'./example/main.js'    // 覆盖掉 webpack.pub.conf的index路径    },    output:{        path: config.build.assetsExpRoot,        filename: '[name].js',   // [name].[chunkhash].js  chunkhash 包hash        library:'hellooo',    // 很重要哦        libraryTarget:'umd'  // 用来兼容common.js AMD等模式, 必须配置，否则会报错（无法引入）    },    plugins:[        new HtmlWebpackPlugin({            filename: 'index.html',   // 文件名称 默认按照output的path来构建            template: 'example/index.html',  // 模版指定位置, template从项目根目录开始            inject: true        }),    ]});module.exports = webpackExpConfig;