/** * Created by lzq on 2017/8/28. * 该配置文件，主要用来增加在开发环境中的 webpack配置 */const path = require('path');const utils = require('./../utils');const webpack = require('webpack');const config = require('../../config/index');const merge = require('webpack-merge');const baseWebpackConfig = require('./webpack.base.conf.js');const HtmlWebpackPlugin = require('html-webpack-plugin');const FriendlyErrorsPlugin = require('friendly-errors-webpack-plugin');baseWebpackConfig.entry = {index: './example/main.js'};Object.keys(baseWebpackConfig.entry).forEach(function(name){    baseWebpackConfig.entry[name] = ['./scripts/dev-client'].concat(baseWebpackConfig.entry[name]);});module.exports = merge(baseWebpackConfig, {    module:{        rules: utils.styleLoaders({sourceMap: config.dev.cssSourceMap})    },    devtool: '#cheap-module-eval-source-map',    plugins: [        new webpack.DefinePlugin({            'process.env': config.dev.env        }),        new webpack.HotModuleReplacementPlugin(),        new webpack.NoEmitOnErrorsPlugin(),        new HtmlWebpackPlugin({            filename: 'index.html',   // 文件名称 默认按照output的path来构建            template: 'example/index.html',  // 模版指定位置            inject: true        }),        new FriendlyErrorsPlugin()    ]});